#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

     int arr[6],sum=0;
    for(int i=0;i<6;i++)
        scanf("%d",&arr[i]);
    
    for(int i=0;i<6;i=i+2)
    {
       sum=sum+(arr[i]*arr[i+1]);
    }
    printf("%d",sum);
    
    return 0;
}
